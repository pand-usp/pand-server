package br.usp.each.pand;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativePlacesResourceIT extends PlacesResourceTest {

    // Execute the same tests but in native mode.
}